---+ TODO List

---++ Types

 * Intersect atomic values
   - Mostly, member constraint has wrong order.
 * Intersect normal types
   - common sub-terms
 * Parameterized types (e.g. list(atomic) /\ list(atom)		[OK]

---+++ Type from concrete values?

 * Value/functor table to enclosing types.
 * type_constraint(:Type, +Value)?

---+++ Type aliases						[OK]

---++ Signatures

?- listing(current_signature). (layout)

